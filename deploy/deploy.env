# 复制为 deploy.env 后修改：
#   cp deploy/deploy.env.example deploy/deploy.env

# 服务器 SSH 登录（必填）
SERVER_HOST=211.159.166.109
SERVER_USER=root

# 可选：指定私钥路径，留空则使用 ssh-agent / 默认密钥
# SSH_KEY=~/.ssh/id_ed25519

# 远程部署目录（与 fitness 应用目录分开）
REMOTE_DIR=/var/www/audit-agent-courseware

# Node 应用内部端口（仅本机监听，不对外暴露）
APP_PORT=3001

# Nginx 对外端口（fitness 占用 80，课件使用 8080）
NGINX_PORT=8080

# PM2 进程名
PM2_APP_NAME=audit-courseware

# 首次部署时是否自动安装 Node 22 / PM2 / Nginx 配置
AUTO_SETUP=true
