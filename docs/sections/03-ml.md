# 03 · ml — ② 特征拟合（ML）

- **锚点：** `#ml`  
- **侧栏：** 特征拟合 ML（15′）  
- **课纲定位：** 对需要拟合、且可人工造特征的问题学参数  

## 教学目标

- 关键词：**特征拟合**  
- 概率用于排序，不是证据  
- 优劣势：能写不尽组合 / 特征靠人、无像素  

## 内容块

1. `KnownUnknownBridge`  
2. `Definition` — 机器学习 · 特征拟合  
3. `SingleCaseAnchor` / 记号条  
4. `FunctionFittingLab` / `InlinePythonLab` ml / `ConfusionMatrixLab`  
5. `CapabilityBoundary` + 优劣势  
6. `DeepDive` + `TrainingLifecycle`  
7. `Bridge` → ANN（超多特征）  

## 对应代码

`app/page.tsx` → `section#ml`
