# 整堂课结构总览

**最后核对代码：** `app/page.tsx` → `export default function Home`
**课程标题：** LLM 与 Agent：基础、架构及审计应用

## 三层总体架构

1. **第一部分：大模型和智能体的技术基础**——规则 → ML → ANN → LLM，解释技术为什么逐层出现。
2. **第二部分：Agent基础与架构**——从LLM的行动缺口出发，讲清Agent的定义、架构、工具反馈、状态、控制和评价。
3. **第三部分：Agent在审计中的应用**——把通用能力组合成受控、可复核、可评价的审计智能体。

三部分不是三个并列术语清单：第一部分建立技术底座，第二部分搭出通用运行系统，第三部分用审计交付、证据和责任边界约束该系统。

## 120分钟安排

| `id` | 章节 | 时间 |
|---|---|---:|
| `problem` | 导言 | 5′ |
| `code` | 基于任务逻辑的编程 | 8′ |
| `ml` | 经典机器学习 | 10′ |
| `nn` | ANN | 12′ |
| `llm` | 从 ANN 到 LLM | 20′ |
| `agent` | Agent：从回答问题到完成任务 | 8′ |
| `agent-architecture` | Agent怎样运行 | 14′ |
| `agent-control` | Agent怎样受控运行 | 8′ |
| `agent-evaluation` | Agent的价值、评价与建设 | 5′ |
| `audit` | 先定义审计功能 | 5′ |
| `audit-architecture` | 智能问数 | 7′ |
| `audit-evidence` | 智能报告生成 | 7′ |
| `audit-rollout` | 共性底座与治理 | 6′ |

正文合计115分钟，预留5分钟用于章节切换与现场停顿。第二、第三部分合计60分钟，是课程重点；以架构图、流程图、分支图和互动演示为主，不设置代码实验。

## 教学主线

```text
导言：课程不是“介绍AI名词”，而是解释解决问题的能力如何升级
  → 第一部分：规则 → ML → ANN → LLM
  → 第二部分 06 定义与边界：行动缺口 → Agent定义 → 适配判定
              07 运行架构：六块架构 → 工具契约 → 状态/反馈/停止 → 单/多Agent
              08 可靠性控制：完整风险面 → 自主度 → 运行前/中/后控制
              09 价值与建设：价值 → 四维评价 → 六阶段建设路径
  → 第三部分 10 功能全景：问数/筛查/取证/底稿/报告/监控 → 场景选择
              11 智能问数：口径契约 → 语义层/权限/安全查询 → 校验 → 可信回答
              12 智能报告：冻结发现 → 结构与起草 → 引用/数字校验 → 人工审批
              13 共性底座：统一证据链 → 治理 → 分功能评价 → 四阶段试点
  → 九题自测与总结
```

## 第一部分：技术基础（`#part-1`）

开场 `#problem` 先展示三部分地图。`#code`—`#llm` 用递进案例解释上一种方法的能力边界：

| 技术 | 事项 | 一句话问题 | 为什么升级 |
|---|---|---|---|
| 规则 | BX-41610 / BX-41902 | 同一张发票报销两次 | 条件可完整表达，规则最合适 |
| ML | BX-41881—84 | 四笔均低于2,000元但集中发生 | 多个弱信号难以穷举 |
| ANN | BX-42306 | 票面286元、平台86元 | 异常藏在高维像素中 |
| LLM | BX-42519 | 客户招待与儿童餐、生日蛋糕、无拜访记录矛盾 | 需要理解语言与业务语境 |

人脸识别保留为ANN趣味支线，不替代审计主线。

## 第二部分：Agent基础与架构（`#part-2`，章节06—09）

不再用一个大section承载全部内容，而是拆为四个独立二级章：

1. **06 从回答到任务（`#agent`）：** LLM行动缺口 → Agent定义 → 程序/工作流/LLM/Agent区分 → 四问适配判定。
2. **07 Agent怎样运行（`#agent-architecture`）：** 六块架构 → 工具契约 → 状态、反馈与停止 → 单Agent/多Agent。
3. **08 Agent怎样受控（`#agent-control`）：** 五类风险 → 自主度三档 → 运行前、运行中、运行后控制。
4. **09 Agent怎样落地（`#agent-evaluation`）：** 业务价值 → 四维评价 → 六阶段建设。

BX-42017仅用于验证动态反馈：航班结果不同会使Agent缩短调查、扩展调查或因工具失败转人工。

## 第三部分：Agent在审计中的应用（`#part-3`，章节10—13）

同样拆为四个独立二级章：

1. **10 定义审计功能（`#audit`）：** 工作产品 → 六类功能 → 技术分工 → 首个场景。
2. **11 智能问数（`#audit-architecture`）：** 五项可信交付、七步流程、四层架构、四种问题分支和LLM/确定性组件分工。
3. **12 智能报告生成（`#audit-evidence`）：** 五类受控输入、八步生成流程、四层架构、完整/缺失/冲突互动和报告人机边界。
4. **13 共性底座与治理（`#audit-rollout`）：** 共性六层架构、统一对象证据链、治理风险、分功能评价、四阶段上线和五方责任。

统一边界：系统可以自动筛查、受控调查和整理证据，不自动认定违规、舞弊或签发审计意见。

第二、第三部分只使用静态图示与互动演示。Python代码实验集中在第一部分，用于解释规则、ML、ANN与LLM底层机制。

## 章节文档

- [01-problem.md](./sections/01-problem.md)
- [02-code.md](./sections/02-code.md)
- [03-ml.md](./sections/03-ml.md)
- [04-nn.md](./sections/04-nn.md)
- [05-llm.md](./sections/05-llm.md)
- [06-agent.md](./sections/06-agent.md)
- [07-audit.md](./sections/07-audit.md)

同文件内未导出的 `LegacyHome`、`ComplexHome` 等不是当前课纲；以导出的 `Home` 为唯一依据。
